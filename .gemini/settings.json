{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_start.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "startup"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_start.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "resume"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_start.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "clear"
      }
    ],
    "BeforeAgent": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"gemini_before_agent.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_end.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "exit"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_end.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "clear"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_end.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "logout"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_end.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "prompt_input_exit"
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node --input-type=module -e 'import fs from '\\''node:fs'\\'';; import path from '\\''node:path'\\'';; import { pathToFileURL } from '\\''node:url'\\'';; const projectDir = process.env.GEMINI_PROJECT_DIR ?? process.env.CLAUDE_PROJECT_DIR ?? process.cwd();; const candidates = [path.resolve(projectDir)];; let cursor = candidates[0];; while (true) {;   const parent = path.dirname(cursor);;   if (parent === cursor) break;;   candidates.push(parent);;   cursor = parent;; }; let hookDir = null;; for (const candidate of candidates) {;   const possible = path.join(candidate, '\\''.memraft'\\'', '\\''hooks'\\'');;   if (fs.existsSync(possible) && fs.statSync(possible).isDirectory()) {;     hookDir = possible;;     break;;   }; }; if (!hookDir) process.exit(0);; const mod = await import(pathToFileURL(path.join(hookDir, \"session_end.mjs\")).href);; const exitCode = await mod.main(process.argv.slice(1));; if (Number.isInteger(exitCode)) process.exit(exitCode);'",
            "timeout": 10
          }
        ],
        "matcher": "other"
      }
    ]
  }
}
